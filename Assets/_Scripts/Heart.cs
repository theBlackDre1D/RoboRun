using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Heart : MonoBehaviour {

    //private int pridatPocetZivitov = 1;
    //private PlayerScript colliderObject;
      
    void Start()
    {

    }
    //void OnTriggerEnter(Collider collider) // Metoda ktora zabezpecuje interakciu 2 objektov ktore maju obidva Collider2D a IsTriggered ma hodnotu true (ak by bol iba Collider, ktory maju iba 3D objekty, tak sa metoda nevykona) tak sa zacinaju vykonavat prikazy  
    //{
    //    colliderObject = collider.gameObject.GetComponent<PlayerScript>(); //z collideru som získal typ(script) a z neho mam teraz pristup k public atributom a metodam v skripte 

    //    if (colliderObject.getPocetZivotov() >= 1 && colliderObject.getPocetZivotov() <= 3) // Ak ma hrac pocet zivotov vacsi alebo rovny 1 a zaroven mensi alebo rovny 3 zvysi sa pocet zivotov o hodnotu pridatPocetZivotov
    //    {
    //        colliderObject.zvysitPocetZivotov(this.pridatPocetZivitov);
    //    }

    //}  
    
    
}
